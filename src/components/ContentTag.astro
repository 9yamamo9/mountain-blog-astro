---
const { contentTags } = Astro.props
---

<div class="gap-3">
	{contentTags && contentTags.map((tag: string) => (
		<span
			class="bg-gray-100 text-gray-600 text-md font-medium me-2 px-2.5 py-0.5 rounded-full dark:bg-gray-700 dark:text-gray-300">
			{tag}
		</span>
	))}
</div>